package com.tom.service.datagen.service;

import org.springframework.stereotype.Component;
import org.springframework.web.filter.ForwardedHeaderFilter;
import com.tom.service.datagen.common.GenerateData;
import com.tom.service.datagen.common.IdGenerator;
import com.tom.service.datagen.dto.SchoolRequest;
import com.tom.service.datagen.exception.AlreadyExistsException;
import com.tom.service.datagen.model.school.Alunos;
import com.tom.service.datagen.model.school.Avaliacoes;
import com.tom.service.datagen.model.school.Disciplinas;
import com.tom.service.datagen.model.school.Matriculas;
import com.tom.service.datagen.model.school.Notas;
import com.tom.service.datagen.model.school.Turmas;

import lombok.extern.log4j.Log4j2;

@Log4j2
@Component
public class SchoolUtils extends GenerateData {

    private final ForwardedHeaderFilter forwardedHeaderFilter;

	private short aluno, disciplina, turma, matricula;

    SchoolUtils(ForwardedHeaderFilter forwardedHeaderFilter) {
        this.forwardedHeaderFilter = forwardedHeaderFilter;
    }

	public Alunos generateSingleAluno() {
		Alunos alunos = new Alunos();
		alunos.setId(null);
		alunos.setMatricula(null);
		alunos.setNome(null);
		return alunos;
	}

	public Disciplinas generateSingleDisciplina() {
		Disciplinas disciplinas = new Disciplinas();
		disciplinas.setId(null);
		disciplinas.setCodigo(null);
		disciplinas.setNome(null);
		return disciplinas;
	}

	public Turmas generateSingleTurma(Disciplinas disciplina) {
		Turmas turmas = new Turmas();
		turmas.setId(null);
		turmas.setId_disciplina(disciplina.getId());
		turmas.setAno(null);
		turmas.setSemestre(null);
		return turmas;
	}

	public Matriculas generateSingleMatricula(Alunos aluno, Turmas turma) {
		Matriculas matriculas = new Matriculas();
		matriculas.setId(null);
		matriculas.setId_aluno(null);
		matriculas.setId_turma(null);
		return matriculas;
	}

	public Avaliacoes generateSingleAvaliacoes() {
		Avaliacoes avaliacoes = new Avaliacoes();
		avaliacoes.setId(null);
		avaliacoes.setId_turma(null);
		avaliacoes.setOrdem(null);
		avaliacoes.setPeso(null);
		avaliacoes.setUnidade(null);
		avaliacoes.setDescricao(null);
		return avaliacoes;
	}

	public Notas generateSingleNotas() {
		Notas notas = new Notas();
		notas.setId(null);
		notas.setId_avaliacao(null);
		notas.setNota(null);
		return notas;
	}

	public void setVariables(SchoolRequest request) {
		log.info("");
		aluno = request.aluno();
		disciplina = request.disciplina();
		turma = request.turma();
		matricula = request.matricula();
	}

}
