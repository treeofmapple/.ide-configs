package ucsal.oferta.academico.model.entity;

import jakarta.persistence.*;
import lombok.Getter;
import lombok.Setter;

import ucsal.oferta.academico.dto.DisciplinaDTO;

@Getter
@Setter
@Entity
public class Disciplina {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    private String codigo;
    private String nome;
    private Integer ch;

    public Disciplina() { }

    public Disciplina(DisciplinaDTO dto) {
        this.id = dto.id();
        this.codigo = dto.codigo();
        this.nome = dto.nome();
        this.ch = dto.ch();
    }
}
