package com.tom.vehicle.normal.mapper;

import com.tom.vehicle.normal.dto.VehicleRequest;
import com.tom.vehicle.normal.dto.VehicleResponse;
import com.tom.vehicle.normal.model.Vehicle;
import javax.annotation.processing.Generated;
import org.springframework.stereotype.Component;

@Generated(
    value = "org.mapstruct.ap.MappingProcessor",
    date = "2025-11-24T01:20:55-0300",
    comments = "version: 1.6.2, compiler: Eclipse JDT (IDE) 3.42.0.v20250526-2018, environment: Java 21.0.7 (Eclipse Adoptium)"
)
@Component
public class VehicleMapperImpl implements VehicleMapper {

    @Override
    public Vehicle build(VehicleRequest request) {
        if ( request == null ) {
            return null;
        }

        Vehicle vehicle = new Vehicle();

        vehicle.setBrand( request.brand() );
        vehicle.setColor( request.color() );
        vehicle.setModel( request.model() );
        vehicle.setPlate( request.plate() );

        return vehicle;
    }

    @Override
    public VehicleResponse toResponse(Vehicle vehicle) {
        if ( vehicle == null ) {
            return null;
        }

        Long id = null;
        String brand = null;
        String model = null;
        String color = null;
        String plate = null;

        id = vehicle.getId();
        brand = vehicle.getBrand();
        model = vehicle.getModel();
        color = vehicle.getColor();
        plate = vehicle.getPlate();

        VehicleResponse vehicleResponse = new VehicleResponse( id, brand, model, color, plate );

        return vehicleResponse;
    }
}
