services:
  basic-api-provider:
    build: .
    container_name: databatch
    restart: unless-stopped
    ports:
      - "${SERVER_PORT}:8000"
    env_file:
      - .env
    
volumes:
  postgres_data:
    driver: local